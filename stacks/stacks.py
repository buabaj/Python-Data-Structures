#code implementation of stacks in the python programming language

#we create the whole stack as class and write the methods to perform functions on items in the stack
class stack:

    def __init__(self): #init method of the class

        self.items = [] #instantiating an empty list so we can perform functions on

    def add(self, item):
        
        self.item.append(item)
